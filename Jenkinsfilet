node { 
         stage('Build') { 
                           withGradle {
                           sh './build.gradle build'
                           }
                         
                      
         } 
}
