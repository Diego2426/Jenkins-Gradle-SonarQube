properties([pipelineTriggers([githubPush()])])

node {
         stage ('Checkout'){
                     git branch: 'master', url: 'https://github.com/Diego2426/Jenkins-Gradle-SonarQube.git'
         }
         stage('Clean') { 
                     steps { 
                            bat 'gradle clean'
                     }
         }
         stage('Build') { 
                     steps { 
                            bat 'gradle build'
                     }
                     /*post {
                        success {
                            echo 'AWESOME SUCCESS!'
                            //emailext body: 'New Update Available', recipientProviders: [[$class: 'DevelopersRecipientProvider'], [$class: 'RequesterRecipientProvider']], subject: 'New Update!'
                        }
                     }*/
         }
}    
