node { 
         stage('Build') { 
                           withGradle {
                           sh 'gradle --version'
                           }
                         
                      
         } 
}
